import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  // Ganti string biasa dengan object
  title: {
    template: "%s | Alim Suma", // %s akan diganti dengan title dari setiap halaman
    default: "Alim Suma | Mobile & Backend Developer", // Title default jika halaman tidak punya title
  },
  description: "Portfolio of Alim Suma, a dedicated Mobile Developer & Backend Engineer.",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}